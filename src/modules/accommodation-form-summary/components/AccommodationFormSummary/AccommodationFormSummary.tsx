import Form from "src/ui-kit/Form";

/**
 * Summary of the accommodation
 * and owner form steps.
 */
function AccommodationFormSummary() {
  const onSubmit = () => {};

  return (
    <Form title="Summary" onSubmit={onSubmit}>
      Form summary
    </Form>
  );
}

export default AccommodationFormSummary;
